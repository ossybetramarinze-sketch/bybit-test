name: Test Bybit Alternative

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test Bytick API
        run: |
          echo "Testing Bybit alternative endpoint..."
          curl -i --max-time 20 \
            "https://api.bytick.com/v5/market/kline?category=linear&symbol=BTCUSDT&interval=5&limit=1"
